# API Base URL for backend
# Vercel prod: leave blank or set to "/api" (frontend + backend share domain)
# Local dev:  set to http://localhost:8000 when running uvicorn directly
VITE_API_BASE_URL=/api

